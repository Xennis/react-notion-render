name: "CI"

on:
  #push:
  #  branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install mise
        uses: jdx/mise-action@v2
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Check code format
        run: pnpm run format:check
      - name: Build packages
        run: pnpm run build --filter @xennis/react-notion-cms-fetch --filter @xennis/react-notion-cms-render
      #- name: Build examples
      #  run: npm run build -w nextjs
